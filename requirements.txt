# betApp (VSPAVIC) — Python 3.10+ recommended
# Install: python -m venv kivy_venv && ./kivy_venv/bin/pip install -r requirements.txt

# UI
Kivy==2.3.1

# Robobidders / BertecMan
numpy==2.4.4
scipy==1.15.2

# Pillow GIF widget (gifWidget)
pillow==10.4.0
